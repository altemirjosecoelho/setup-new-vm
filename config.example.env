# Configurações de Ambiente para Jenkins Pipeline
# Este arquivo serve como exemplo das variáveis que devem ser configuradas no Jenkins
# NÃO inclua credenciais reais neste arquivo!

# =============================================================================
# CONFIGURAÇÕES DE CONEXÃO SSH
# =============================================================================
VM_HOST=192.168.1.100                    # IP da VM de destino
VM_USER=jenkins                          # Usuário SSH na VM
SSH_KEY_PATH=/var/lib/jenkins/.ssh/id_rsa # Caminho da chave SSH

# =============================================================================
# CREDENCIAIS DE BANCO DE DADOS
# =============================================================================
POSTGRES_PASSWORD=sua_senha_postgres_aqui
MONGODB_PASSWORD=sua_senha_mongodb_aqui
PGADMIN_EMAIL=admin@seudominio.com
PGADMIN_PASSWORD=sua_senha_pgadmin_aqui
PORTAINER_PASSWORD=sua_senha_portainer_aqui

# =============================================================================
# CONFIGURAÇÕES DE DOMÍNIO (opcional)
# =============================================================================
TRAEFIK_DOMAIN=traefik.seudominio.com
PORTAINER_DOMAIN=portainer.seudominio.com
PGADMIN_DOMAIN=pgadmin.seudominio.com

# =============================================================================
# INSTRUÇÕES DE CONFIGURAÇÃO NO JENKINS
# =============================================================================
# 1. Acesse o Jenkins > Gerenciar Jenkins > Configurar o Sistema
# 2. Na seção "Variáveis de ambiente globais", adicione as variáveis acima
# 3. Ou configure as variáveis no nível do job/pipeline
# 4. Para credenciais sensíveis, use o sistema de Credentials do Jenkins
# 5. Nunca commite credenciais reais no repositório Git

# =============================================================================
# EXEMPLO DE CONFIGURAÇÃO COM CREDENTIALS DO JENKINS
# =============================================================================
# No pipeline, você pode usar:
# withCredentials([string(credentialsId: 'postgres-password', variable: 'POSTGRES_PASSWORD')]) {
#     // seu código aqui
# }
